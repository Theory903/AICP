// AicpConfig is defined in `runtime::AicpConfig` (alongside the other
// RuntimeFeatureConfig fields). This module is intentionally empty — all
// config types live in the `runtime` crate to avoid a dependency cycle.
